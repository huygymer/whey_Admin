# Medici Admin

Change API URL in `env` file

In the project directory, you can run:

# Installation

```bash
$ yarn install

# Running the app
```bash
$ yarn dev

# build

```bash
$ yarn build

# start
```bash
$ yarn start
